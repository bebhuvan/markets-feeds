name: Feed Fetcher

on:
  schedule:
    # 6:00 AM IST = 12:30 AM UTC
    - cron: '30 0 * * *'
    # 3:00 PM IST = 9:30 AM UTC  
    - cron: '30 9 * * *'
  workflow_dispatch: # Manual trigger

jobs:
  fetch:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: 'v2/package-lock.json'
    
    - name: Verify project structure
      run: |
        pwd
        ls -la
        echo "=== v2 directory ==="
        cd v2
        ls -la
        echo "=== package.json ==="
        cat package.json
    
    - name: Install dependencies
      working-directory: v2
      run: |
        echo "Installing npm dependencies..."
        npm ci
        echo "Dependencies installed"
    
    - name: Run feed fetcher
      working-directory: v2
      run: |
        echo "Starting RSS feed fetch..."
        npm run fetch-feeds
        echo "Feed fetch completed"
      env:
        NODE_ENV: production
    
    - name: Check for new content
      run: |
        echo "Checking git status..."
        git status
        echo "=== Content directory ==="
        ls -la v2/content/links/
    
    - name: Commit new feeds
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        
        # Check if there are any changes to commit
        if [[ -n $(git status --porcelain v2/content/links/) ]]; then
          echo "Found changes, committing..."
          git add v2/content/links/*.json
          
          # Add report if it exists
          if [[ -f "v2/fetch-report.json" ]]; then
            git add v2/fetch-report.json
          fi
          
          git commit -m "🤖 Auto-update feeds $(date -u +'%Y-%m-%d %H:%M UTC')"
          git push
          echo "✅ Successfully pushed new feeds"
        else
          echo "ℹ️ No new feeds to commit"
        fi
    
    - name: Upload artifacts
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: fetch-report
        path: |
          v2/fetch-report.json
          v2/*.log
        retention-days: 5
        if-no-files-found: ignore